<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.1.xsd
        http://www.liquibase.org/xml/ns/dbchangelog-ext
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <changeSet id="1" author="badape">

        <createTable tableName="hero">
            <column name="hero_id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="game_id" type="bigint">
                <constraints unique="false" nullable="false"/>
            </column>
            <column name="credits" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="description" type="varchar(50)">
                <constraints nullable="false"/>
            </column>
            <column name="health" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="mana" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="stamina" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="spawn_cost" type="bigint">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql>
            ALTER TABLE heroes.hero ADD CONSTRAINT hero_credits_check CHECK (credits >= 0)
        </sql>
        <sql>
            ALTER TABLE heroes.hero ADD CONSTRAINT hero_health_check CHECK (credits >= 0)
        </sql>
        <sql>
            ALTER TABLE heroes.hero ADD CONSTRAINT hero_mana_check CHECK (credits >= 0)
        </sql>
        <sql>
            ALTER TABLE heroes.hero ADD CONSTRAINT hero_stamina_check CHECK (credits >= 0)
        </sql>
        <sql>
            ALTER TABLE heroes.hero ADD CONSTRAINT hero_spawn_cost_check CHECK (credits >= 0)
        </sql>

    </changeSet>

</databaseChangeLog>